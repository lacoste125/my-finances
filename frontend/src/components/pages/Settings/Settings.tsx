import React from "react";

const Settings: React.FC = () => {
    return (
        <div>
            <h1>Ustawienia</h1>
            <p>Tu będą ustawienia użytkownika.</p>
        </div>
    );
};

export default Settings;
