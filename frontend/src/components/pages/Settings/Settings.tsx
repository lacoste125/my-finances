import React from "react";

export const Settings: React.FC = () => {
    return (
        <div>
            <h1>Ustawienia</h1>
            <p>Tu będą ustawienia użytkownika.</p>
        </div>
    );
};
