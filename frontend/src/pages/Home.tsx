import React from "react";
import Counter from "../features/counter/Counter";

const Home: React.FC = () => {
    return (
        <div>
            <h1>Strona główna</h1>
            <Counter/>
        </div>
    );
};

export default Home;
